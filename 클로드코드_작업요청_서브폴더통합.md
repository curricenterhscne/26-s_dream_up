# 클로드 코드 작업 요청 — 수강신청 웹앱을 온마당(onmadang.or.kr) 서브폴더로 통합

## 배경 및 목표
현재 두 개의 별도 깃허브 레포가 있습니다.
1. **온마당 레포** — onmadang.or.kr 사이트 (신규 구축 중)
2. **수강신청 웹앱 레포** — 기존에는 꿈키움 학교 밖 교육(88강좌) 선착순 수강신청용,
   최근 2026학년도 2학기 고교-대학 연계 학점 인정 학교 밖 교육 과목(14강좌)용
   콘텐츠로 교체 작업 중인 앱

이 수강신청 웹앱을 **온마당 사이트의 서브폴더**(예: onmadang.or.kr/apply 같은
경로)로 접근할 수 있게 만들고 싶습니다. 별도 도메인/서브도메인이 아니라
**같은 도메인의 하위 경로**로 통합하는 것이 목표입니다.

## 1단계 — 조사 (먼저 이것부터 해주세요)
두 레포를 각각 열어서 아래를 확인해 주세요. 저장소 구조에 따라 통합 방법이
완전히 달라지므로, 실행하기 전에 먼저 파악이 필요합니다.

- **온마당 레포**
  - 어떤 방식으로 빌드/배포되는가? (예: GitHub Pages 정적 배포, Jekyll,
    Next.js/Vite 등 빌드 도구 사용 여부, 별도 서버로 배포하는 GitHub Actions
    워크플로우 존재 여부, PHP/그누보드 코드가 포함되어 있는지 여부)
  - 배포 결과물이 정적 파일(HTML/CSS/JS)인지, PHP 등 서버 실행이 필요한
    코드인지
  - 리포지토리 루트 구조 (배포되는 디렉터리가 루트인지 `docs/`, `dist/`,
    `public/` 등 별도 폴더인지)
  - 커스텀 도메인 설정 파일(`CNAME` 등) 존재 여부

- **수강신청 웹앱 레포**
  - 어떤 프레임워크로 만들어졌는가 (React/Vite, plain HTML 등)
  - 빌드 명령어와 빌드 결과물 위치
  - 빌드 결과물 안의 경로 참조가 절대경로(`/assets/...`)로 되어 있는지,
    상대경로로 되어 있는지 — 서브폴더에 배치하면 절대경로 참조가 깨질 수
    있으니 base path 설정이 필요한지 확인 (예: Vite의 `base` 옵션,
    React의 `homepage` 필드 등)
  - 현재 어디에 배포되어 있는지(Cloudflare Pages 등)와 그 설정

## 2단계 — 통합 방법 판단 및 제안
조사 결과에 따라 아래 중 가장 적합한 방법을 판단해서 저에게 먼저 제안해
주시고, 확인 후 진행해 주세요. (파일을 삭제하거나 기존 배포를 깨뜨릴 수
있는 작업이므로 실행 전 확인 요청)

- **(A) 같은 레포·정적 배포인 경우 (가장 간단)**: 수강신청 앱을 빌드해서
  나온 정적 결과물을 온마당 레포의 서브폴더(예: `apply/`)에 그대로 복사해
  커밋합니다. 온마당 레포가 GitHub Pages 등으로 배포되고 있다면 이 폴더가
  자동으로 `onmadang.or.kr/apply/`에서 서비스됩니다. 앱의 asset 경로를
  `/apply/` 기준 상대/절대 경로로 맞추는 base path 설정이 필요합니다.

- **(B) 두 레포를 계속 분리해서 유지하고 싶은 경우**: 온마당 레포에
  GitHub Actions 워크플로우를 하나 추가해서, 수강신청 앱 레포가
  업데이트될 때(또는 온마당 레포 배포 시점마다) 앱 레포를 체크아웃 →
  빌드 → 결과물을 온마당 레포의 `apply/` 폴더에 넣고 커밋/배포하는 방식.
  두 레포를 각자 독립적으로 계속 개발하면서도 배포물만 합치고 싶을 때
  적합합니다.

- **(C) 온마당이 실제 PHP(그누보드) 등 서버 실행형이라 정적 GitHub Pages로
  안 되는 경우**: 서버에 두 프로젝트를 각각 올리고(그누보드는 기존대로,
  수강신청 앱은 정적 파일로 서브폴더 경로에) 웹서버(Nginx/Apache) 설정으로
  `/apply` 경로만 정적 파일 폴더를 바라보게 하는 방식이 필요합니다. 이
  경우 서버 설정 파일(vhost 설정 등)에 대한 접근 권한이 있는지 다시
  확인해 주세요.

- **(D) 위 방법들이 여의치 않을 때의 대안**: 온마당 사이트 안에 수강신청
  앱을 iframe으로 임베드하는 페이지를 하나 만드는 방법도 있습니다. 다만
  주소창에 실제로 `/apply` 경로가 보이지 않거나(iframe 내부 URL은
  그대로), 모바일에서 스크롤/높이 처리가 부자연스러울 수 있어 후순위로
  고려해 주세요.

## 3단계 — 실행
제안한 방법에 대해 제 확인을 받은 뒤 진행해 주세요. 진행 시:
- 기존 온마당 레포의 다른 페이지/자산을 건드리지 않도록 주의
- 수강신청 앱의 API 호출/데이터 제출 경로(있다면)가 서브폴더 배치 후에도
  정상 동작하는지 확인
- 완료 후 로컬 빌드로 `/apply/` 경로가 정상적으로 열리는지 확인해 보고
  결과를 알려주세요

## 참고
- 수강신청 웹앱에 들어갈 콘텐츠(14개 강좌 데이터)는 별도로
  `courses_for_webapp.json`과 `클로드코드_작업요청.md`로 이미 전달했습니다.
  이번 요청은 그 내용과는 별개로, **배포 위치(서브폴더 통합)**에 대한
  것입니다. 두 작업을 함께 진행해도 되고 순서를 나눠도 됩니다.
